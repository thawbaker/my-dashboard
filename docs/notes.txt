
adding new fields to a  kanban task: start date/time 
end date/time
estimated duration (hh:mm:ss)
actual duration (hh:mm:ss)
variance +/-(hh:mm:ss)

these will be editable fields in task editable

there will be two additional buttons on the task edit form (work and complete) 

the first time the work button is pressed the start
date/time of now will be populated and the contents of the task edit form are saved and flushed to the db. then a dialog fashioned after the jpeg in docs will pop up; start will start a work timer and pause will stop it; if the timer is stoped or the dialog is closed the timer value should be added to actual duration; saved and synced to the db.



